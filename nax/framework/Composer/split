rm -rf ../Empty/ ../Nonempty/
find -L -mindepth 3 -type d|while read d; do if find -L "$d" -name ".*.cpl" -print -quit|grep . -q; then r=../Nonempty; else r=../Empty; fi; dst="$r/`dirname \"$d\"`"; mkdir -p "$dst" && cp -d "$d" "$dst"; done


